# Pinned exactly: the dashboard uses use_container_width, which Streamlit
# deprecated with removal scheduled after 2025-12-31. An open >= pin would let
# a fresh install pull a release that has removed it and break every page.
# 1.58.0 is the version the dashboard is verified against.
streamlit==1.58.0
pandas>=2.0
numpy>=1.24
plotly>=5.18
mlxtend>=0.23
scikit-learn>=1.3
openpyxl>=3.1
networkx>=3.0
# Postgres driver for the data platform (OLTP loader, warehouse ETL, and the
# dashboard's live mode). The dashboard falls back to the CSV artifacts if
# Postgres is not running, so this is not required just to view the dashboard.
psycopg2-binary>=2.9

