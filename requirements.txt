streamlit>=1.30
pandas>=2.0
numpy>=1.24
plotly>=5.18
mlxtend>=0.23
scikit-learn>=1.3
openpyxl>=3.1
networkx>=3.0
# Postgres driver for the data platform (OLTP loader, warehouse ETL, and the
# dashboard's live mode). The dashboard falls back to the CSV artifacts if
# Postgres is not running, so this is not required just to view the dashboard.
psycopg2-binary>=2.9

