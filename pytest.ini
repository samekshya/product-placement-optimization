[pytest]
# One command runs the whole suite from the repository root:
#     venv/Scripts/python -m pytest
testpaths =
    app/api/tests
    dashboard/tests
    analysis/tests
norecursedirs = venv node_modules .git logs data
